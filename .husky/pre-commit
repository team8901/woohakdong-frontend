echo "🔍 커밋 이전에 lint 검사를 진행합니다..."

if npx lint-staged; then
  echo "✅ lint 검사에 성공했습니다."
  exit 0
else
  echo "❌ lint 검사에 실패했습니다. lint fix 후에 커밋해 주세요."
  exit 1
fi