import { CardHeader, CardTitle } from '@workspace/ui/components/card';

export const RegisterClubCardHeader = () => {
  return (
    <CardHeader>
      <CardTitle>새로운 동아리를 등록해봐요! 😍</CardTitle>
    </CardHeader>
  );
};
